name: web-direct
version: 1.0.0
description: "Credential-free direct HTTP extraction for bounded HTML and plain-text pages. Extract-only; preserves Hermes SSRF, redirect, DNS-rebinding, and website-policy protections."
author: NousResearch
kind: backend
provides_web_providers:
  - direct
